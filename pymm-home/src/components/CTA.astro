---
const docsUrl = import.meta.env.DEV
  ? "http://localhost:8000"
  : "https://docs.example.com";
---

<section class="cta">
  <div class="container">
    <div class="cta-content">
      <h2 class="cta-title">Ready to optimize your Python applications?</h2>
      <p class="cta-description">
        Join thousands of developers who have already improved their application performance with pymm.
      </p>
      <div class="cta-actions">
        <a href={docsUrl} class="button button-primary">
          Get Started Now
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <line x1="7" y1="17" x2="17" y2="7"></line>
            <polyline points="7,7 17,7 17,17"></polyline>
          </svg>
        </a>
        <a href="https://github.com/your-org/pymm" class="button button-secondary">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"></path>
          </svg>
          View on GitHub
        </a>
      </div>
    </div>
    <div class="cta-stats">
      <div class="stat-card">
        <div class="stat-number">50K+</div>
        <div class="stat-label">Downloads</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">2.1K</div>
        <div class="stat-label">GitHub Stars</div>
      </div>
      <div class="stat-card">
        <div class="stat-number">98%</div>
        <div class="stat-label">Satisfaction</div>
      </div>
    </div>
  </div>
</section>

<style>
  .cta {
    padding: 6rem 0;
    background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
    color: white;
  }

  .container {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .cta-title {
    font-size: 2.5rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1rem;
  }

  .cta-description {
    font-size: 1.125rem;
    color: #cbd5e1;
    margin-bottom: 2rem;
    line-height: 1.7;
  }

  .cta-actions {
    display: flex;
    gap: 1rem;
  }

  .button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s ease;
    border: 2px solid transparent;
  }

  .button-primary {
    background: #2563eb;
    color: white;
  }

  .button-primary:hover {
    background: #1d4ed8;
    transform: translateY(-1px);
    box-shadow: 0 10px 25px rgba(37, 99, 235, 0.3);
  }

  .button-secondary {
    background: transparent;
    color: white;
    border-color: #475569;
  }

  .button-secondary:hover {
    border-color: white;
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-1px);
  }

  .cta-stats {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .stat-card {
    background: rgba(255, 255, 255, 0.1);
    padding: 1.5rem;
    border-radius: 1rem;
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
  }

  .stat-number {
    display: block;
    font-size: 2rem;
    font-weight: 800;
    color: #60a5fa;
    margin-bottom: 0.25rem;
  }

  .stat-label {
    color: #cbd5e1;
    font-size: 0.875rem;
    font-weight: 500;
  }

  @media (max-width: 1024px) {
    .container {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .cta-stats {
      flex-direction: row;
      justify-content: center;
    }
  }

  @media (max-width: 768px) {
    .cta {
      padding: 4rem 0;
    }

    .cta-title {
      font-size: 2rem;
    }

    .cta-actions {
      flex-direction: column;
      align-items: center;
    }

    .button {
      width: 200px;
      justify-content: center;
    }

    .cta-stats {
      flex-direction: column;
      max-width: 300px;
      margin: 0 auto;
    }
  }
</style>
